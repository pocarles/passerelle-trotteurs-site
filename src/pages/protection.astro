---
import Base from "../layouts/Base.astro";
import PageHeader from "../components/PageHeader.astro";
import Icon from "../components/Icon.astro";
import { intakeProfiles, protections } from "../data/site";
---

<Base
  title="La protection Passerelle — Un contrat pour la vie"
  description="Découvrez les six protections contractuelles qui accompagnent à vie chaque Trotteur Français reconverti par Passerelle."
>
  <PageHeader
    eyebrow="Confier & protéger"
    title="Un cadre sécurisé et sécurisant pour les protéger et les suivre"
    accent="pour le reste de leur vie."
    lead="La protection Passerelle s’applique à tous les chevaux reconvertis par l’association, quel que soit leur parcours après les courses."
    video="/media/mecenes-preparation-setf.mp4"
    background
  />

  <section class="section why">
    <div class="wrap why__grid">
      <div class="why__intro">
        <p class="eyebrow">Pourquoi choisir Passerelle&nbsp;?</p>
        <h2>Une prise en charge <span class="accent">pensée pour leur avenir.</span></h2>
      </div>
      <div class="why__content">
        <p>
          Notre partenariat avec la SETF permet d’accompagner chaque Trotteur
          dès sa sortie des courses. Il bénéficie d’une reconversion adaptée,
          d’une protection contractuelle et d’un suivi tout au long de sa vie.
        </p>
        <ul class="why__list">
          <li><strong>Un réseau reconnu</strong> de professionnels formés à la reconversion.</li>
          <li><strong>Un contrat protecteur</strong> qui encadre chaque placement.</li>
          <li><strong>Un suivi à vie</strong> pour ne jamais perdre la trace du cheval.</li>
        </ul>
        <a
          class="btn btn--primary"
          href="https://intranet.passerelle-trotteurs.fr/user/login?destination=/accueil"
          target="_blank"
          rel="noreferrer"
        >
          Faire une demande de prise en charge
          <span class="btn__ico"><Icon name="arrow" /></span>
        </a>
      </div>
    </div>
  </section>

  <section class="section profiles-sec">
    <div class="wrap">
      <p class="eyebrow">Propriétaires · Entraîneurs · Éleveurs</p>
      <h2 class="profiles__title">Pour quels chevaux&nbsp;?</h2>
      <ul class="profiles">
        {
          intakeProfiles.map((profile) => (
            <li>
              <span class="badge"><Icon name="horse" /></span>
              <h3>{profile.title}</h3>
              <p>{profile.body}</p>
            </li>
          ))
        }
      </ul>
    </div>
  </section>

  <section class="notice-sec">
    <div class="wrap">
      <div class="notice">
        <span class="notice__ico"><Icon name="info" /></span>
        <div>
          <h3>Nos capacités d’accueil sont actuellement limitées.</h3>
          <p>
            Nos structures sont saturées. Les chevaux peuvent être inscrits sur
            liste d’attente, sans garantie de délai. Passerelle peut également
            fournir un contrat protecteur pour encadrer un placement direct.
          </p>
        </div>
        <a class="btn btn--ghost" href="/contact">
          Nous contacter
          <span class="btn__ico"><Icon name="arrow" /></span>
        </a>
      </div>
    </div>
  </section>

  <section class="section benefits">
    <div class="wrap">
      <p class="eyebrow">Une garantie partagée</p>
      <h2 class="benefits__title">Protéger le cheval, <span class="accent">sécuriser chaque partie.</span></h2>
      <div class="benefits__grid">
        <article>
          <figure class="benefits__image">
            <img
              src="/media/protection/professionnels-courses-trot.jpg"
              alt="Trotteur en course de trot attelé"
              loading="lazy"
            />
          </figure>
          <div class="benefits__heading">
            <span class="badge"><Icon name="horse" /></span>
            <h3>Pour les professionnels des courses</h3>
          </div>
          <p>
            Le professionnel sait que son cheval passe entre les mains d’une
            équipe formée à la reconversion, qui connaît le monde des courses,
            notamment l’entraînement des chevaux de course. Il bénéficie de la
            garantie contractuelle de la meilleure «&nbsp;seconde vie&nbsp;»
            possible pour son cheval, à l’issue de sa carrière.
          </p>
        </article>
        <article>
          <figure class="benefits__image">
            <img
              src="/media/pathway-adoption.jpg"
              alt="Cavalière de club avec un Trotteur Français"
              loading="lazy"
            />
          </figure>
          <div class="benefits__heading">
            <span class="badge"><Icon name="heart" /></span>
            <h3>Pour les cavaliers</h3>
          </div>
          <p>
            L’opportunité d’acheter en toute tranquillité, sous contrat, un
            cheval qui a bénéficié des meilleurs soins et d’un travail adapté à
            ses capacités physiques et mentales.
          </p>
        </article>
      </div>
    </div>
  </section>

  <section class="section protect">
    <div class="protect__lock" aria-hidden="true">
      <span class="protect__lock-shackle"></span>
      <span class="protect__lock-body"></span>
    </div>
    <div class="wrap protect__grid">
      <div>
        <p class="eyebrow eyebrow--light">Six protections</p>
        <h2>La protection Passerelle <span class="accent--light">pour tous.</span></h2>
      </div>
      <ol class="protect__list">
        {
          protections.map((protection, index) => (
            <li>
              <span class="protect__num">{String(index + 1).padStart(2, "0")}</span>
              <span>{protection}</span>
            </li>
          ))
        }
      </ol>
    </div>
  </section>
</Base>

<style>
  .why {
    background: var(--cream-warm);
    border-bottom: 1px solid var(--line);
  }
  .why__grid {
    display: grid;
    grid-template-columns: 0.8fr 1.2fr;
    gap: clamp(2.5rem, 8vw, 7rem);
    align-items: start;
  }
  .why h2 {
    max-width: 14ch;
  }
  .why__content > p {
    margin: 0;
    max-width: 58ch;
    color: var(--ink-soft);
    font-size: 1.08rem;
    line-height: 1.65;
  }
  .why__list {
    display: grid;
    gap: 0.85rem;
    margin: 1.8rem 0 2rem;
    padding: 0;
    list-style: none;
  }
  .why__list li {
    padding-left: 1.15rem;
    border-left: 2px solid var(--sage);
    color: var(--ink-soft);
  }
  .why__list strong {
    color: var(--ink);
  }

  .profiles__title {
    margin-bottom: 3rem;
  }
  .profiles {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: clamp(1.8rem, 4vw, 3rem);
    margin: 0;
    padding: 0;
    list-style: none;
  }
  .profiles h3 {
    margin: 1.2rem 0 0.5rem;
  }
  .profiles p {
    margin: 0;
    color: var(--ink-soft);
  }
  .notice-sec {
    padding-block: 0 clamp(3rem, 6vw, 4.5rem);
  }
  .notice {
    display: flex;
    align-items: center;
    gap: clamp(1.2rem, 3vw, 2.4rem);
    padding: clamp(1.6rem, 3vw, 2.2rem);
    border: 1px solid var(--line);
    border-left: 3px solid var(--moss-mid);
    border-radius: var(--radius);
    background: var(--cream-warm);
  }
  .notice__ico {
    display: grid;
    place-items: center;
    flex: 0 0 auto;
    color: var(--moss);
  }
  .notice__ico :global(svg) {
    width: 30px;
    height: 30px;
  }
  .notice h3 {
    margin-bottom: 0.35rem;
    font-family: var(--sans);
    font-size: 1.02rem;
    font-weight: 600;
    letter-spacing: 0;
  }
  .notice p {
    max-width: 62ch;
    margin: 0;
    color: var(--ink-soft);
    font-size: 0.95rem;
  }
  .notice .btn {
    flex: 0 0 auto;
  }

  .protect {
    position: relative;
    isolation: isolate;
    overflow: hidden;
    background: var(--forest-deep);
    color: #fff;
  }
  .protect__grid {
    position: relative;
    z-index: 1;
    display: grid;
    grid-template-columns: 0.75fr 1.25fr;
    gap: clamp(2.5rem, 7vw, 6rem);
    align-items: start;
  }
  .protect__lock {
    position: absolute;
    z-index: 2;
    bottom: clamp(1rem, 3vw, 3rem);
    left: clamp(1rem, 5vw, 5rem);
    width: clamp(11rem, 17vw, 19rem);
    aspect-ratio: 0.78;
    opacity: 0.16;
    pointer-events: none;
  }
  .protect__lock-shackle {
    position: absolute;
    top: 0;
    left: 18%;
    width: 64%;
    height: 59%;
    border: clamp(8px, 0.75vw, 12px) solid var(--sage);
    border-bottom: 0;
    border-radius: 50% 50% 0 0 / 62% 62% 0 0;
    box-shadow: inset 0 0 0 clamp(1px, 0.16vw, 3px) rgba(255, 255, 255, 0.18);
    transform-origin: 85% 100%;
    animation: protect-lock-close 7s cubic-bezier(0.4, 0, 0.2, 1) infinite;
  }
  .protect__lock-body {
    position: absolute;
    right: 0;
    bottom: 0;
    left: 0;
    height: 61%;
    border: clamp(8px, 0.75vw, 12px) solid var(--sage);
    border-radius: 12% 12% 8% 8%;
    background: linear-gradient(135deg, rgba(186, 214, 136, 0.3), rgba(186, 214, 136, 0.1));
    box-shadow:
      inset 0 clamp(3px, 0.35vw, 6px) 0 rgba(255, 255, 255, 0.18),
      inset 0 -4px 0 rgba(0, 0, 0, 0.14);
  }
  .protect__lock-body::before {
    position: absolute;
    top: 34%;
    left: 50%;
    width: clamp(0.9rem, 1.55vw, 1.4rem);
    height: clamp(1.25rem, 2.25vw, 2rem);
    border-radius: 50% 50% 40% 40%;
    background: var(--forest-deep);
    content: "";
    transform: translateX(-50%);
  }
  .protect__lock-body::after {
    position: absolute;
    top: calc(34% + clamp(0.9rem, 1.55vw, 1.4rem));
    left: 50%;
    width: clamp(0.35rem, 0.6vw, 0.55rem);
    height: clamp(1.15rem, 1.9vw, 1.7rem);
    border-radius: 0 0 999px 999px;
    background: var(--forest-deep);
    content: "";
    transform: translateX(-50%);
  }
  @keyframes protect-lock-close {
    0%,
    20% {
      transform: translateY(-24%) rotate(-8deg);
    }
    40%,
    80% {
      transform: translateY(0) rotate(0);
    }
    100% {
      transform: translateY(-24%) rotate(-8deg);
    }
  }
  @media (prefers-reduced-motion: reduce) {
    .protect__lock-shackle {
      animation: none;
      transform: translateY(0);
    }
  }
  .protect h2 {
    color: #fff;
  }
  .protect__list {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1px;
    margin: 0;
    padding: 0;
    list-style: none;
    overflow: hidden;
    border: 1px solid var(--line-light);
    border-radius: var(--radius);
    background: var(--line-light);
  }
  .protect__list li {
    display: flex;
    align-items: flex-start;
    gap: 1rem;
    padding: 1.5rem 1.4rem;
    background: var(--forest-deep);
  }
  .protect__num {
    flex: 0 0 auto;
    color: var(--sage);
    font-family: var(--serif);
  }
  .benefits__title {
    max-width: 22ch;
    margin-bottom: 3rem;
  }
  .benefits__grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: clamp(2rem, 5vw, 4rem);
  }
  .benefits article {
    position: relative;
    padding-top: 0;
  }
  .benefits__image {
    aspect-ratio: 16 / 9;
    margin: 0;
    overflow: hidden;
    border-radius: var(--radius);
    background: var(--cream-warm);
  }
  .benefits__image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }
  .benefits article .badge {
    flex: 0 0 auto;
  }
  .benefits__heading {
    display: flex;
    align-items: center;
    gap: 0.8rem;
    margin: 0.8rem 0 0.7rem;
  }
  .benefits h3 {
    margin: 0;
    color: var(--moss);
  }
  .benefits p {
    max-width: 50ch;
    color: var(--ink-soft);
  }

  @media (max-width: 860px) {
    .why__grid,
    .profiles,
    .protect__grid,
    .benefits__grid {
      grid-template-columns: 1fr;
    }
    .protect__lock {
      left: 1rem;
      bottom: 1rem;
      width: 9rem;
    }
  }
  @media (max-width: 720px) {
    .notice {
      flex-direction: column;
      align-items: flex-start;
      border-radius: var(--radius-lg);
    }
  }
  @media (max-width: 620px) {
    .protect__list {
      grid-template-columns: 1fr;
    }
  }
</style>
