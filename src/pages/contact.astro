---
import Base from "../layouts/Base.astro";
import PageHeader from "../components/PageHeader.astro";
import Icon from "../components/Icon.astro";
import { links, site } from "../data/site";

const doors = [
  {
    icon: "hand",
    title: "Confier & protéger",
    body: "Découvrir les conditions de prise en charge et la protection proposée par Passerelle.",
    label: "Découvrir la prise en charge",
    href: "/confier-et-proteger",
    external: false,
  },
  {
    icon: "heart",
    title: "Adopter un trotteur",
    body: "Découvrir les formules de vente, d’adoption et de placement direct.",
    label: "Découvrir l’adoption",
    href: "/adopter",
    external: false,
  },
  {
    icon: "sprout",
    title: "Faire un don",
    body: "Soutenir les soins, le transport et la reconversion des chevaux.",
    label: "Faire un don",
    href: links.donate,
    external: true,
  },
  {
    icon: "info",
    title: "Presse et partenariats",
    body: "Écrire directement à l’association pour présenter votre demande.",
    label: "Écrire à Passerelle",
    href: `mailto:${site.email}?subject=Presse%20ou%20partenariat`,
    external: false,
  },
];
---

<Base
  title="Contact — Association Passerelle"
  description="Contactez l’Association Passerelle pour une prise en charge, une adoption, un don, une demande presse ou un partenariat."
>
  <PageHeader
    class="contact-header"
    eyebrow="Parlons-nous"
    title="Trouver la bonne porte,"
    accent="dès le premier message."
    lead="Pour accélérer le traitement de votre demande, choisissez ci-dessous le parcours qui correspond à votre situation."
  />

  <section class="section routes">
    <div class="wrap">
      <p class="eyebrow">Votre demande</p>
      <h2 class="routes__title">Comment pouvons-nous vous aider&nbsp;?</h2>
      <div class="routes__grid">
        {
          doors.map((door) => (
            <article>
              <span class="badge"><Icon name={door.icon} /></span>
              <h3>{door.title}</h3>
              <p>{door.body}</p>
              <a
                class="tlink"
                href={door.href}
                target={door.external ? "_blank" : undefined}
                rel={door.external ? "noreferrer" : undefined}
              >
                {door.label}
                <Icon name={door.external ? "arrowUpRight" : "arrow"} />
              </a>
            </article>
          ))
        }
      </div>
    </div>
  </section>

  <section class="section details">
    <div class="wrap details__grid">
      <div>
        <p class="eyebrow eyebrow--light">Coordonnées</p>
        <h2>Association <span class="accent--light">Passerelle</span></h2>
      </div>
      <address>
        <p>
          {site.address.street}<br />
          {site.address.postalCode} {site.address.city}<br />
          {site.address.country}
        </p>
        <a href={`tel:${site.phone}`}>{site.phoneDisplay}</a>
        <a href={`mailto:${site.email}`}>{site.email}</a>
      </address>
      <div class="social">
        <p class="eyebrow eyebrow--light">Nous suivre</p>
        <a href={links.facebook} target="_blank" rel="noreferrer">
          Facebook <Icon name="arrowUpRight" />
        </a>
        <a href={links.instagram} target="_blank" rel="noreferrer">
          Instagram <Icon name="arrowUpRight" />
        </a>
      </div>
    </div>
  </section>
</Base>

<style>
  :global(.contact-header) {
    position: relative;
    overflow: hidden;
    isolation: isolate;
  }
  :global(.contact-header)::before,
  :global(.contact-header)::after {
    content: "";
    position: absolute;
    z-index: -1;
    pointer-events: none;
  }
  :global(.contact-header)::before {
    top: 26%;
    left: -8rem;
    width: 6.5rem;
    height: 3.4rem;
    background: var(--sage);
    clip-path: polygon(0 18%, 100% 0, 61% 49%, 100% 100%, 44% 63%, 0 100%, 28% 50%);
    filter: drop-shadow(0 0.5rem 0.65rem rgba(31, 61, 43, 0.12));
    opacity: 0.55;
    transform: rotate(-5deg);
    animation: contact-paper-plane 13s linear infinite;
  }
  :global(.contact-header)::after {
    top: calc(26% + 1.8rem);
    left: -13rem;
    width: 6rem;
    height: 1px;
    background: repeating-linear-gradient(90deg, var(--sage) 0 0.45rem, transparent 0.45rem 0.85rem);
    opacity: 0.45;
    transform: rotate(-5deg);
    animation: contact-paper-plane-trail 13s linear infinite;
  }
  @keyframes contact-paper-plane {
    0% {
      left: -8rem;
      transform: translateY(1.2rem) rotate(-5deg);
    }
    45% {
      transform: translateY(-1rem) rotate(3deg);
    }
    100% {
      left: calc(100% + 2rem);
      transform: translateY(0.6rem) rotate(-2deg);
    }
  }
  @keyframes contact-paper-plane-trail {
    0% {
      left: -13rem;
      transform: translateY(1.2rem) rotate(-5deg);
    }
    45% {
      transform: translateY(-1rem) rotate(3deg);
    }
    100% {
      left: calc(100% - 2rem);
      transform: translateY(0.6rem) rotate(-2deg);
    }
  }

  .routes__title {
    margin-bottom: 3rem;
  }
  .routes__grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1px;
    overflow: hidden;
    border: 1px solid var(--line);
    border-radius: var(--radius-lg);
    background: var(--line);
  }
  .routes article {
    padding: clamp(1.5rem, 4vw, 2.5rem);
    background: var(--card);
  }
  .routes h3 {
    margin: 1.3rem 0 0.55rem;
  }
  .routes article p {
    min-height: 3.2em;
    color: var(--ink-soft);
  }
  .details {
    background: var(--forest-deep);
    color: #fff;
    /* tighter than .section: this band carries three short columns, and the
       full section rhythm leaves a large empty area above the footer */
    padding-block: clamp(3rem, 5.5vw, 4.5rem);
  }
  .details__grid {
    display: grid;
    grid-template-columns: 1fr 1fr 0.7fr;
    gap: clamp(2rem, 6vw, 5rem);
    align-items: start;
  }
  .details h2 {
    color: #fff;
  }
  .details address {
    display: grid;
    gap: 0.7rem;
    font-style: normal;
  }
  .details address p {
    margin: 0 0 0.5rem;
    color: rgba(255, 255, 255, 0.75);
  }
  .details address a,
  .social a {
    overflow-wrap: anywhere;
    color: var(--sage);
  }
  .social {
    display: grid;
    gap: 0.7rem;
  }
  .social .eyebrow {
    margin-bottom: 0.4rem;
  }
  .social a {
    display: inline-flex;
    align-items: center;
    gap: 0.45rem;
  }
  .social :global(svg) {
    width: 15px;
    height: 15px;
  }

  @media (max-width: 780px) {
    .details__grid {
      grid-template-columns: 1fr;
    }
  }
  @media (max-width: 640px) {
    .routes__grid {
      grid-template-columns: 1fr;
    }
    .routes article p {
      min-height: 0;
    }
  }
  @media (prefers-reduced-motion: reduce) {
    :global(.contact-header)::before,
    :global(.contact-header)::after {
      animation: none;
      left: auto;
      right: 8%;
    }
  }
</style>
